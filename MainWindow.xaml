<Window x:Class="WPFGAME.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFGAME"
        xmlns:viewModels="clr-namespace:WPFGAME.ViewModels"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="AliceBlue"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Height="1080" Width="1920">
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Border Grid.Row="0" Background="#FF0078D7" MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="—" Width="30" Command="{Binding MinimizeCommand}" />
                <Button Content="X" Width="30" Command="{Binding CloseCommand}" />
            </StackPanel>
        </Border>

        <!-- Your Main Content -->
        <ContentControl Grid.Row="1" Content="{Binding SelectedViewModel}"/>
    </Grid>

</Window>
