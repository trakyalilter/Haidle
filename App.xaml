<Application x:Class="WPFGAME.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WPFGAME"
             xmlns:viewmodels="clr-namespace:WPFGAME.ViewModels"
             xmlns:views="clr-namespace:WPFGAME.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <DataTemplate DataType="{x:Type viewmodels:GameViewModel}">
            <views:MainGame/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ShopVM}">
            <views:Shop/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:FarmingViewModel}">
            <views:Farming/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ForestViewModel}">
            <views:Forest/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MiningViewModel}">
            <views:Mining/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:InventoryViewModel}">
            <views:Inventory/>
        </DataTemplate>
        
    </Application.Resources>
</Application>
